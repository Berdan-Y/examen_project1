{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <p>Categorieen</p>
            </div>
        </div>
        <div class="row">
            {% for category in categories %}
                <div class="col-4">
                    <a class="btn btn-primary" href="{{ path('app_member_treatments', {'id': category.id}) }}">{{ category.name }}</a>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}